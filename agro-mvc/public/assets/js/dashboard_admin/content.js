class Content {
    static tables = Array.from(document.querySelectorAll("table"));
    static buttons = Array.from(document.querySelectorAll("button"));
    static poup = document.querySelector("dialog");  
}

export default Content;